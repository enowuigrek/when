/**
 * The panel's card surface and its section heading.
 *
 * Both strings had been retyped in about forty places, which is how three
 * different heading treatments — uppercase-wide, uppercase-widest, and plain
 * grey — ended up sitting on pages next to each other.
 */
export const card = "rounded-xl border border-zinc-800/60 bg-zinc-900/40";

/** The small grey label above a block of content. */
export const sectionHeading = "text-xs font-medium uppercase tracking-wider text-zinc-500";
